# Stack

The binary is Rust and intentionally has no workspace configuration schema.
It reads native files such as `package.json`, `pnpm-workspace.yaml`,
`Cargo.toml`, `pyproject.toml`, `go.mod`, and Compose files. Policy is TOML in
`.airis/policies.toml`; AI distribution is owned by AIris Code.
